# 规范质量检查清单：Agent 前端交互页面

**目的**：在进入计划阶段前校验规范完整性与质量
**创建日期**：2026-09-10
**功能**：[spec.md](../spec.md)

## 内容质量

- [x] 不含实现细节（编程语言、框架、API）
- [x] 聚焦用户价值与业务需求
- [x] 面向非技术相关方编写
- [x] 所有必填章节已完成

## 需求完整性

- [x] 无 [NEEDS CLARIFICATION] 标记残留
- [x] 需求可测试且无歧义
- [x] 成功标准可衡量
- [x] 成功标准与技术无关（不含实现细节）
- [x] 所有验收场景已定义
- [x] 已识别边界情况
- [x] 范围已清晰界定
- [x] 已识别依赖与假设

## 功能就绪度

- [x] 所有功能需求均有明确验收标准
- [x] 用户场景覆盖主要流程
- [x] 功能满足成功标准中定义的可衡量结果
- [x] 规范中未泄漏实现细节

## 备注

- **全部检查项通过**，可进入 `/speckit.plan`。
- 规范正文（用户故事 / 功能需求 / 成功标准）不含实现细节；文末"后端契约对齐"章节为**依赖追溯附表**（记录功能点与后端契约的对应关系），不作为前端实现规定。
- 2026-09-10 目录口径确认：空间目录按后端白名单统一为 **9 个**（生产计划、产线信息、切换时间、求解时间、产线电价、目标优先级、使用规则、shared、tmp），"加号上传入口""@ 文件选择""工作空间文件"三处展示的目录集合完全一致；原需求"8 个空间文件夹"的表述以本口径为准，澄清标记已消除（对应 `SC-021`）。
- 2026-09-10 需求修订后重新校验通过：工具调用仅展示工具名、思考输出默认折叠、快速模式=未开启思考、会话内禁止连续发送（发送按钮置灰）、上传失败提示原因并提醒重试、MCP 失败仅显示状态、会话进行中禁止切换数字人（按钮置灰）、历史默认 10 条+"更多"查看最近 100 次、外部地址直接跳转/空间目录文件右侧展示、搜索黄色高亮逐次跳转、回复失败或超时在聊天区显示错误信息。
- 2026-09-10 新增功能校验通过：输入"@"引用空间文件（展示空间目录、可从各文件夹选择文件、以"@文件名"插入且实际以"目录 + 文件名"结构化提交）。
- 2026-09-10 契约对齐校验通过：依据后端 `specs/002-agent-chat-ui/contracts/` 与 `docs/api.md` 完成对齐——目录白名单、思考与工具调用不落历史、@ 引用结构化提交（≤10）、消息状态与用量字段、请求级模型与思考参数、数字人选定/退出与进行中禁止切换、中断本轮、上传与预览限制、SSE 断连语义。
- 2026-09-10 **后端源码核对**：进一步对照 `agent-backend/src/routes/*.ts`、`src/domain/*.ts` 逐接口核对，修正 4 处规范口径——① 目录白名单确认为 **9 个**（后端契约文档"10 个"为笔误）；② `GET /api/threads` **不支持分页参数**，10/100 条改为前端切片（FR-039/FR-040 仍可满足）；③ `THREAD_BUSY_LIMIT` 在"新建会话"时为**会话总数上限 3**（非并发），"并发上限"表述已修正；④ "会话进行中禁止切换数字人"后端**未校验**，为纯前端 UX 约束（后端 spec `FR-021` 的实现缺口）。详见 `contracts/backend-api.md` §7。
- 2026-09-10 **会话上限口径修正（第 ③ 条作废）**：`POST /api/threads` **取消会话总数上限**，`THREAD_BUSY_LIMIT` 改为只由发消息接口按**并发**（同一用户活跃 run ≥ 3，**按 userId 跨数字人累计**）返回。详见 `contracts/backend-api.md` §7 差异 6。
