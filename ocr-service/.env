# OCR 容器配置（入库，无密钥）。由 docker-compose.yml 的 env_file 注入容器。
# 本机私产（LAN IP 等）放 .env.local——同样会注入，并覆盖本文件同名项。
# 可调项（下载上限、超时）见 .env.example。

# 回源下载白名单：逗号分隔，字符串精确匹配。容器形态只需服务名 backend。
OCR_URL_ALLOW_HOSTS=backend
