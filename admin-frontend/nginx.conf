# 纯静态托管：反代职责在 gateway 容器
# 资源以 `/admin/` 为基址（vite `base`），故 root 指向 dist 并通过 try_files 回退到 index.html。
server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA 路由回退（管理界面用 History API，刷新任意子路径都需回退到 index.html）
    location / {
        try_files $uri $uri/ /index.html;
    }
}
