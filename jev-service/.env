# Jev 容器配置（入库，无密钥）。由 docker-compose.yml 的 env_file 注入容器。
# 本机私产（API Key、LAN IP）放 .env.local——同样会注入，并覆盖本文件同名项。
# 可调项（上游端点、超时重试、state 上限）见 .env.example。

# 回源下载白名单：逗号分隔，字符串精确匹配。容器形态只需服务名 backend。
JEV_URL_ALLOW_HOSTS=backend
